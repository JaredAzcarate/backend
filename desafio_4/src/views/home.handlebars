<div class="m-5 gap-5 bg-gray-100 rounded-lg p-5">
    <p class="text-xl font-bold mb-5">Esta es la lista de todos los productos:</p>

        <ul>    
        {{#each products}}
            <li class="flex gap-4 w-3/6 justify-between hover:bg-black hover:text-white rounded-md p-2 transition-all">
                <div>
                    <p>{{id}}</p>
                    <p>{{title}}</p>
                </div>
            </li>
        {{/each}}
        </ul>

</div>